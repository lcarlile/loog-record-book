Superseded. These built the first version of the record book from the spreadsheet CSV
plus hand-assembled JSON, before `refresh.js` could derive everything from ESPN.

They are kept only as provenance for the figures now in `data/loog/`. Nothing builds
from them, and they will not run against the current layout.
